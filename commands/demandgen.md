---
description: Build a Demand Gen campaign (audience + creative) (claude-google-ads suite).
---

Use the `claude-google-ads:builder-demand-gen` skill to build a Demand Gen campaign for the business in the
current working directory.

**Use ONLY `claude-google-ads:builder-demand-gen`.** Do not invoke other ads skills — this command is
dedicated to the claude-google-ads suite.

Audience strategy + image/video creative sets (via the assets skill); note the upper-funnel measurement
caveats. Emit `blueprint.xlsx` + `campaign-spec.json` (campaign_type: demand_gen).

$ARGUMENTS
